#include <stdio.h>

int main() {
    int x = 2;
// since 0 means false 
// therefore the condition invloving 0 returns false

    if(x=0) {
        printf("x is 1 \n");
        printf("%d \n", x);
    }
    else {
        printf("x is not 1 \n");
    }
    return 0;
}