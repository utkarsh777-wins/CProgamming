#include <stdio.h>

int main() {
    // output logic Q
    int x = 2;
    if(x = 1) {
        printf("Equals to one");
    }
    else {
        printf("Not equals to one");
    }
    return 0;
}
// Equals to one 
// for comparison we use ==
// so basically x=1 replaced 2 in the x container by reassingment
