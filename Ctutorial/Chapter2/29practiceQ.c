#include <stdio.h>

int main() {
    // WAP to scheck if it's sunday amd snowing

    // declaring variables with initial value as true(1)
    int isSunday = 0;
    int isSnowing = 1;


    // will print true only if both the conditions are true
    printf("%d", isSunday && isSnowing);
    return 0;
}