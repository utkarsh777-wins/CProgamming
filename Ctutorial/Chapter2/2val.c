#include <stdio.h>

int main() {
    
    // int oldAge = 22;
    // int newAge = oldAge + years;
    // int years = 2;   invalid flow

    int oldAge = 20;
    int years = 1;
    int newAge = oldAge + years;
    // now that's a valid flow 

    return 0;
}